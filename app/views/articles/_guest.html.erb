<% @current_articles.each do |article| %>
  <tr>
    <td class="text-align: center"><%= article.likes %>&#9825;</td>
    <td><%= article.title %></td>
    <td><%= button_to 'Like', like_path(article), method: :put %></td>
  </tr>
<% end %>